spring:
 application:
  name: payment
 data:
  mongodb:
    database: payment
    host: localhost
    port: "27017"
    uri: mongodb://localhost/payment
    username: ""
eureka:
 client:
  register-with-eureka: "true"
  fetch-registry: "true"
  serviceUrl:
    defaultZone: http://localhost:9084/eureka/
server:
 port: "9081"
